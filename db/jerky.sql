-- DROP DATABASE IF EXISTS jerky_DB;

-- CREATE DATABASE jerky_DB;

-- USE jerky_DB;

-- CREATE TABLE products (
--     id INT NOT NULL AUTO_INCREMENT,
--     item_name VARCHAR(50) NOT NULL, 
--     starting_price DECIMAL(10,2), 
--     current_high_bid DECIMAL(10,2),
--   PRIMARY KEY (id)
-- );
-- INSERT INTO products (item_name, starting_price, current_high_bid)
-- item_name VALUES ("paco taco", 2.50, 100);

-- INSERT INTO products (item_name, starting_price, current_high_bid)
-- starting_price VALUES ("used tennis ball", 3.10, 120);

-- INSERT INTO products (item_name, starting_price, current_high_bid)
-- current_high_bid VALUES ("human face", 3.25, 75);